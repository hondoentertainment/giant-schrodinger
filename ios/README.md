# iOS Xcode project

Generated by `npx cap add ios`. Do not sign this folder in CI.

```bash
npm run ios:sync    # vite build + cap sync ios
npx cap open ios    # Mac + Xcode
```

Icons and splash in `App/App/Assets.xcassets` are copied from `store/ios/` (`npm run ios:assets`). Signing, TestFlight, and App Review: [store/OWNER_STEPS.md](../store/OWNER_STEPS.md).
